<h2>登場人物一覧</h2> 
<h3><%= link_to "作成",new_person_path %></h3>

<ul>
  <% @people.each do |person|%>

    <%= link_to person_path(person.id) do %>
      <li>
        <%= person.name %>
      </li>
    <% end %>
  <% end %>

</ul>