<%= @tag.name %>

<%= @tag.description %>

<%= @tag.person.name %>

<%= @tag.id %>

  <%= link_to '編集',edit_tag_path(@tag) %>
  <%= link_to '削除',tag_path(@tag),
    method: :delete,
    data: {confirm: '本当に削除しますか？'}
  %>